# Angular + .NET + Docker Boilerplate - Environment Variables Example
# Copy this file to .env.local and adjust values as needed
# The boilerplate works perfectly without any environment configuration!

# =============================================================================
# FRONTEND (Angular) ENVIRONMENT VARIABLES
# =============================================================================

# API URL for frontend to connect to backend
# Default: http://localhost/api (through nginx proxy)
# For direct backend access: http://localhost:8080/api
NG_APP_API_URL=http://localhost/api

# Application environment
NG_APP_ENV=development

# Enable/disable production optimizations
NODE_ENV=production

# Angular build configuration
NG_BUILD_CONFIGURATION=production

# =============================================================================
# BACKEND (.NET) ENVIRONMENT VARIABLES  
# =============================================================================

# ASP.NET Core environment
ASPNETCORE_ENVIRONMENT=Development

# Server URLs that ASP.NET Core will listen on
ASPNETCORE_URLS=http://+:8080

# Database connection string (PostgreSQL)
# Format: Host=hostname;Port=port;Database=dbname;Username=user;Password=pass
ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=boilerplate_db;Username=boilerplate_user;Password=boilerplate_pass

# Logging configuration
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft=Warning
Logging__LogLevel__Microsoft.Hosting.Lifetime=Information

# JWT Secret for authentication (if implementing auth)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS settings
CORS__AllowedOrigins=http://localhost,http://localhost:4200,http://localhost:80

# =============================================================================
# DATABASE (PostgreSQL) ENVIRONMENT VARIABLES
# =============================================================================

# PostgreSQL database configuration
POSTGRES_DB=boilerplate_db
POSTGRES_USER=boilerplate_user  
POSTGRES_PASSWORD=boilerplate_pass

# =============================================================================
# DOCKER COMPOSE OVERRIDES
# =============================================================================

# Uncomment these in docker-compose.yml under the respective services
# to enable environment file usage

# Example docker-compose.yml usage:
# services:
#   frontend:
#     env_file:
#       - .env.local
#     environment:
#       - NG_APP_API_URL=${NG_APP_API_URL:-http://localhost/api}
#
#   backend:
#     env_file:
#       - .env.local  
#     environment:
#       - ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT:-Development}